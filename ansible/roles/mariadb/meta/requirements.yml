---
# Install these collections with ansible-galaxy; collections are deliberately
# not vendored in this repository.
collections:
  - name: ansible.mariadb
    version: ">=6.0.0"
  - name: ansible.posix
    version: ">=1.3.0"
