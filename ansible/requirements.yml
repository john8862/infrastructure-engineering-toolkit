---
# The final repository uses one explicit collection set for CI and examples.
# Collections are installed outside the checkout; nothing is vendored here.
collections:
  - name: ansible.mariadb
    version: 6.0.2
  - name: ansible.posix
    version: 2.2.2
  - name: community.general
    version: 13.0.0
